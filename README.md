# ssh-bruteforce-detector
Real-time SSH brute force detection tool in Python. Monitors Ubuntu auth logs, detects repeated failed login attempts from the same IP, and simulates automatic blocking with auto-unban support. Includes configurable thresholds, whitelist, incident logging, and persistent blocked IP storage for lab-based security testing and SOC practice.
